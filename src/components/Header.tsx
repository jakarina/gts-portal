import Head from "next/head";

function Header() {
  return (
    <Head>
      <title>GTS Portals</title>
      <meta name="description" content="GTS Portal" />
      <link rel="icon" href="/favicon.ico" />
    </Head>
  );
}

export default Header;
